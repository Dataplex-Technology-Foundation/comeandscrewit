# Pinned to exact versions (not ranges) so a newly-published upstream
# release can't be pulled in automatically without review. Bump
# deliberately, e.g. `pip install --upgrade requests beautifulsoup4 lxml`
# then re-pin the resolved versions here.
requests==2.32.5
beautifulsoup4==4.14.3
lxml==6.1.0
